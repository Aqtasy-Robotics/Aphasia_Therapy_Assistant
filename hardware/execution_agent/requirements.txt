# ── Raspberry Pi Dependencies ──
# (Pi only records audio, runs GUI, and POSTs to laptop server)
# All agent/AI/LangGraph code runs on the laptop — not needed here.
 
# Core
requests>=2.32.0           # HTTP POST audio to laptop server
python-dotenv>=1.0.0       # .env file loading (SAMPLE_RATE, RECORD_SECONDS etc.)
pydantic>=2.5.0            # Data validation
pydantic-settings>=2.2.1   # Pydantic BaseSettings for env + config
 
# Audio
sounddevice>=0.4.6         # Pi mic recording + speaker playback
soundfile>=0.12.1          # Save/read WAV files
numpy>=1.26.0              # Audio buffer handling (required by sounddevice)
 
# UI – Kivy (7" Touchscreen)
kivy>=2.3.0                # Touch UI framework
 
# Hardware – GPIO & Servos
gpiozero>=2.0              # Servo control
# RPi.GPIO                 # Auto-installed on Pi, fallback mocked
 
# Hardware – OLED Display
luma.oled>=3.13.0          # SSD1351 / SH1106 OLED driver
luma.core>=2.4.0           # Core rendering for luma
Pillow>=10.2.0             # Image rendering for OLED faces
 
# Utilities
loguru>=0.7.0              # Logging (used in body_app.py)
httpx>=0.27.0              # Async HTTP (kept for any existing hardware drivers)